// @ts-ignore
import configData from "./config.json";

export const config = {
    "avatarURL": configData.avatarURL,
    "userLookupURL": configData.userLookupURL,
    "routerMode": configData.routerMode,
    "useMockRallyAPI": configData.useMockRallyAPI
}
